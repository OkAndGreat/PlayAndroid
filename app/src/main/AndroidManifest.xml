<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.playandroid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_playandroid"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlayAndroid"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.activity.PostArticleActivity"></activity>
        <activity android:name=".ui.activity.HistoryActivity" />
        <activity android:name=".ui.activity.CollectionActivity" />
        <activity android:name=".ui.activity.PointActivity" />
        <activity android:name=".ui.activity.SearchActivity" />
        <activity android:name=".ui.activity.RegisterActivity" />
        <activity android:name=".ui.activity.LoginActivity" />
        <activity android:name=".ui.activity.OpenProjectActivity" />
        <activity android:name=".ui.activity.ThatDayActivity" />
        <activity android:name=".ui.activity.GirlAndJokesActivity" />
        <activity android:name=".ui.activity.WebActivity" />
        <activity android:name=".ui.activity.AboutAuthorActivity" />
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>